{$H+}       { +long ANSI strings -short strings }
{$T-}       { +typed addresses | -untyped pointer }
{$B+}       { evaluation boolean expressions +short-cutted | -full }
{$I+}       { io check }
{$R+}       { range check }
{$MODE OBJFPC} 
{$OPTIMIZATION ON}